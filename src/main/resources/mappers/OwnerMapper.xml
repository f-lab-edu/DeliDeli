<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="flab.delideli.dao.OwnerDao" >

    <update id="updateOrderStatusCooking">
        UPDATE orders
        SET order_status = 'START_COOKING'
        WHERE order_id = #{orderId}
    </update>

    <select id="getShopIdInShops">
        SELECT shop_id
        FROM shops
        WHERE owner_id = #{userId}
    </select>
    
    <select id="getShopIdInOrders">
        SELECT shop_id
        FROM orders
        WHERE order_id = #{orderId}
    </select>

    <update id="updateOrderStatusCancel">
        UPDATE orders
        SET order_status = 'ORDER_CANCEL'
        WHERE order_id = #{orderId}
    </update>
</mapper>