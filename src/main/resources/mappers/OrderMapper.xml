<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="flab.delideli.dao.MenuDao" >

    <insert id="insertOrder" >
      INSERT INTO orders (user_id, address, phone_number, total_price, payment_method, request, order_date, shop_id)
      VALUES (#{userId}, #{address}, #{phoneNumber}, #{totalPrice},
              #{paymentMethod}, #{request}, #{orderDate}, #{shopId});
    </insert>
  
    <insert id="insertOrderMenus" parameterType="java.util.List"></insert>

</mapper>