<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="flab.delideli.repository.MemberDAO">
    <select id="selectMembers" parameterType="flab.delideli.repository.MemberDAO"
            resultType="flab.delideli.repository.MemberDAO">
        SELECT * FROM members
    </select>
</mapper>